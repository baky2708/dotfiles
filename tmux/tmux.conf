# Pane switching
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

# Swap for last window
bind -r ^ last-window

# set -g default-terminal "xterm"

#Delay
set -sg escape-time 20
